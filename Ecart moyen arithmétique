//Paramètres de dispersion 

//Ecart-moyen arithmétique

function [e]=EcartMoyenArith(tab,type_data) //Création d'une fonction nommée EcartMoyenArith prenant en arguments d'entrée un tableau contenant des données et éventuellement des poids sur ces données, et un type de données (groupées, non groupées, etc). La valeur de l'écart moyen sera enregistrée et retournée sous le nom de variable 'e'.

data=tab(1,:) //On récupère la 1ere colonne du tableau, celle qui contient les données. Dans le cas de données groupées le tableau dispose d'une deuxième colonne contenant le poids des valeurs.
n=length(data) //Permet de récupérer le nombre de valeurs
Xmoy=mean(data) //Fait la moyenne sur toutes les valeurs

somme=0 //OBLIGATOIRE lorsque l'on fait une accumulation

if type_data==1 then //Cas de données NON-GROUPEES
    for i=1:n
        somme=somme+abs(data(i)-Xmoy) //Etape de la somme : E(|xi-X|)    E:Somme cumulée
    end
    
elseif type_data==2 then //Cas de données GROUPEES
    data_weight=tab(2,:) //Récupération des poids ni stockées dans la deuxième colonne du tableau
    for i=1:n
        somme=somme+data_weight(i)*abs(data(i)-Xmoy) //Etape de la somme : E(ni|xi-X|)
    end
    
elseif type_data==3 then //Cas de données CONTINUES
    //A venir
end

e=(1/n)*somme //Enfin on divise par le nombre de valeurs : e=(1/n)*somme

endfunction
